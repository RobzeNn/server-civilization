# Database (used by apps/api and packages/db migrations)
DB_HOST=localhost
DB_PORT=5432
DB_USER=server_civilization
DB_PASSWORD=server_civilization
DB_NAME=server_civilization

# API secrets (copy into apps/api/.env)
# DISCORD_CLIENT_ID=
# DISCORD_CLIENT_SECRET=
# DISCORD_OAUTH_REDIRECT_URI=
# SESSION_SECRET=

# Bot secrets (copy into apps/bot/.env)
# DISCORD_BOT_TOKEN=
